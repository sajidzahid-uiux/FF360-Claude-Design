export enum ButtonVariantEnum {
  DEFAULT = 'default',
  SURFACE = 'surface',
  GHOST = 'ghost',
  DELETE = 'delete',
  DANGER = 'danger',
  ACCENT = 'accent',
  COLORED = 'colored',
}

export type ButtonVariant = ButtonVariantEnum;
