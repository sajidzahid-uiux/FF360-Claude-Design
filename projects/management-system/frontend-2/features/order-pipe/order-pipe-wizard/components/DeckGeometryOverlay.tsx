"use client";

/** Shim — geometry layers live in features/map. */
export {
  DeckGeometryLayers as DeckGeometryOverlay,
  type DeckGeometryLayersProps as DeckGeometryOverlayProps,
} from "@/features/map/ui";
