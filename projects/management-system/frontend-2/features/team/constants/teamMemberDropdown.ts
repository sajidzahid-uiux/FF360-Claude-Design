/** Menu panel: wide enough for email line + two badges on typical screens */
export const TEAM_MEMBER_DROPDOWN_CONTENT_CLASS =
  "min-w-[min(100vw-2rem,28rem)]";

/** Closed trigger: fits cropped username + two badges */
export const TEAM_MEMBER_DROPDOWN_TRIGGER_CLASS =
  "min-w-[min(100%,17rem)] sm:min-w-[19rem]";

/** Max characters shown in dropdown trigger before ellipsis (username / fallback name). */
export const TEAM_MEMBER_TRIGGER_NAME_MAX = 20;
